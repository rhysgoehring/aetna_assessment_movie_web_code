import React from "react";
import { useDispatch, useSelector } from "react-redux";

const HoverBox = props => {
  return (
    <div className="hoverbox">Hoverbox</div>
  )
};

export default HoverBox;
